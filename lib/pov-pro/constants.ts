// Shared constants for the POV Pro avatar landing pages.

/**
 * Product display name. Single source of truth so a rename (e.g. "POV Pro" vs
 * "POV Engine") is a one-line change across every landing page.
 */
export const PRODUCT_NAME = 'POV Pro'

/**
 * GoHighLevel booking widget. Every CTA opens this in an in-page popup
 * (see components/pov-pro/BookingContext.tsx) rather than a new tab.
 */
export const BOOKING_IFRAME_SRC =
  'https://start.cruciblecoaching.org/widget/booking/Rk0lQk4DhR27dvsXyJuP'
export const BOOKING_IFRAME_ID = 'Rk0lQk4DhR27dvsXyJuP_1781541124461'
export const BOOKING_EMBED_SCRIPT = 'https://start.cruciblecoaching.org/js/form_embed.js'

/** Brand orange — used for per-page themeColor metadata. */
export const BRAND_THEME_COLOR = '#FF8800'
